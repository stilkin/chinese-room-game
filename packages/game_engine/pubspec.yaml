name: game_engine
description: Pi-Ying game engine — board representation, rules, clone brain, similarity search, and narration.
publish_to: none

environment:
  sdk: ^3.7.0

resolution: workspace

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
